# Web CLI Crawler
Basic integration for web crawler using cli

# What's needed

- node

# Setup

1. Install node modules
    ```
    > npm install
    ```
2. build the typescript

    ```
    > npm run tsc
    ```

3. Start the app
    ```
    > npm start
    ```
